import { TablePreset } from '@gandogames/lib/common/game-table';

/** Battleship's table look — a neutral, rectangular surface — used by the lobby's seated view. */
export const BATTLESHIP_TABLE_PRESET: TablePreset = {
	variant: 'neutral',
	shape: 'rectangle',
	label: 'Battleship',
};
