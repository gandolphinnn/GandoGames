import { TablePreset } from '@gandogames/lib/common/game-table';

/** Pankov's table look — a neutral themed surface — shared by the lobby and the in-game view. */
export const PANKOV_TABLE_PRESET: TablePreset = {
	variant: 'neutral',
	seats: 6,
};
