import { TablePreset } from '@gandogames/lib/common/game-table';

/** Poker's table look — a green felt surface — shared by the lobby and the in-game view. */
export const POKER_TABLE_PRESET: TablePreset = {
	variant: 'felt',
	label: "Texas Hold'em",
};
